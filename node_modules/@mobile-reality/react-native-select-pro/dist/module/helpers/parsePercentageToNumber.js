export const parsePercentageToNumber = phrase => {
  if (phrase.includes('%')) {
    const split = phrase.split('%');
    return Number(split[0]);
  }

  return 0;
};
//# sourceMappingURL=parsePercentageToNumber.js.map