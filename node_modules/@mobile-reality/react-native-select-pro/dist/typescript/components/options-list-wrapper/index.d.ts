import { ComponentProps, ReactNode } from 'react';
import { StyleProp, ViewStyle } from 'react-native';
import type { OptionalToRequired } from '../../helpers/types/OptionalToRequired';
import type { OptionsList } from '../options-list';
declare type FromOptionListProps = Pick<ComponentProps<typeof OptionsList>, 'animated' | 'animationDuration' | 'isOpened'>;
declare type WrapperStyles = {
    wrapperStyles: StyleProp<ViewStyle>;
};
declare type OptionsListWrapperProps = OptionalToRequired<FromOptionListProps> & {
    children: ReactNode;
} & WrapperStyles;
export declare const OptionsListWrapper: ({ children, animated, animationDuration, isOpened, wrapperStyles, }: OptionsListWrapperProps) => JSX.Element | null;
export {};
