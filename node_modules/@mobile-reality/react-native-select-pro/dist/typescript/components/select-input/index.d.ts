import type { OptionalToRequired } from '../../helpers/types/OptionalToRequired';
import { DispatchType, State } from '../../state/types';
import type { OnPressSelectControlType, OnSetPosition, SelectProps } from '../../types';
declare type SelectInputProps = OptionalToRequired<Pick<State, 'isOpened' | 'searchValue' | 'selectedOption'> & {
    dispatch: DispatchType;
} & Pick<SelectProps, 'placeholderText' | 'searchPattern' | 'disabled' | 'multiSelection' | 'selectControlTextStyle' | 'placeholderTextColor'> & {
    onPressSelectControl: OnPressSelectControlType;
} & {
    setPosition: OnSetPosition;
}>;
export declare const SelectInput: ({ disabled, isOpened, searchValue, searchPattern, placeholderText, onPressSelectControl, dispatch, setPosition, multiSelection, selectedOption, placeholderTextColor, selectControlTextStyle, }: SelectInputProps) => JSX.Element;
export {};
