declare type Size = {
    size: string | number | undefined;
    sizeType: 'width' | 'height';
    screenSize: number;
    sizeFallback: number;
};
export declare const getSize: ({ size, sizeType, screenSize, sizeFallback }: Size) => number;
export {};
