"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getSize = void 0;

var _heightPercentageToDP = require("./heightPercentageToDP");

var _widthPercentageToDP = require("./widthPercentageToDP");

const getSize = _ref => {
  let {
    size,
    sizeType,
    screenSize,
    sizeFallback
  } = _ref;

  if (typeof size === 'undefined') {
    return sizeFallback;
  }

  if (typeof size === 'number') {
    return size ?? sizeFallback;
  }

  if (typeof size === 'string') {
    if (sizeType === 'width') {
      return (0, _widthPercentageToDP.widthPercentageToDP)(size, screenSize);
    }

    if (sizeType === 'height') {
      return (0, _heightPercentageToDP.heightPercentageToDP)(size, screenSize);
    }
  }

  return sizeFallback;
};

exports.getSize = getSize;
//# sourceMappingURL=getSize.js.map